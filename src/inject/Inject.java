package inject;

import data.GiveawayDatabase;
import data.PayerDatabase;
import data.Transaction;

public class Inject 
{
	public static void transaction(GiveawayDatabase gaDatabase, PayerDatabase pDatabase)
	{
		//To delete from sql (wipes payer): delete from payers where COL_NAME="pemdara";
		//Example
		//pDatabase.processTransaction(new Transaction("Aug 7, 2013", "12:26pm", "animelovers21", "datrade", "", 1));
		/*
		pDatabase.processTransaction(new Transaction("Nov 9, 2013", "1:11am", "Marcylittleninja", "datrade", "Commission :thumb412704719: Digital Art for $20 to help donate to the Philippines. ", 100));
		pDatabase.processTransaction(new Transaction("Nov 9, 2013", "1:11am", "Rice-Taiga", "datrade", "For your donation pool! ", 100));
		pDatabase.processTransaction(new Transaction("Nov 9, 2013", "1:11am", "iskarlata", "datrade", "fav llama", 100));
		pDatabase.processTransaction(new Transaction("Nov 9, 2013", "1:11am", "magibubble", "datrade", "fav llama", 10));
		pDatabase.processTransaction(new Transaction("Nov 9, 2013", "1:11am", "myumyu-myuzu", "datrade", "fav", 20));
		pDatabase.processTransaction(new Transaction("Nov 9, 2013", "1:11am", "SplatterMutt", "datrade", "For your donation pool!", 10));
		pDatabase.processTransaction(new Transaction("Nov 9, 2013", "1:11am", "IAnimeEmma", "datrade", "fav", 10));
		pDatabase.processTransaction(new Transaction("Nov 9, 2013", "1:11am", "Ocean-Amber", "datrade", "watch", 20));
		
		*/
	
		
		
		
		
		System.exit(0);
	}
}
